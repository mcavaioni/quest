
<%= collection_check_boxes :survey, :question_ids, Question.select{|a| a.title == title_ajax}, :id, :content do |cb| %>
              <% cb.label(class: "checkbox-inline hide-checkbox") {cb.check_box(class: "checkbox") + cb.text} %>
              <% end %>
