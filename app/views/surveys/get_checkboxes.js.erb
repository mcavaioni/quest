
//check if the survey already exists (therefore it's an update) and I don't need to append the lista again
if($(".check").children().length <= 0){
  <% Question::Titles.each do |title| %>

    $(".check").append($("<%= j render partial: 'ajax', locals: {title_ajax: title} %>").hide());

  <%end%>
}